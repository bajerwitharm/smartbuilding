select count(*) AS connections, name, account, ((to_char(connections.timestamp,'H'))) as hour from users left join connections on connections.user_id=users.id where result='OK' {1} group by hour, mac order by connections desc;