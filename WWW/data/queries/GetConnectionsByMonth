select count(*) AS connections, name, account, (DATE_FORMAT(connections.timestamp,'%m')) as month from users left join connections on connections.user_id=users.id where result='OK' {1} group by month, mac order by connections desc;