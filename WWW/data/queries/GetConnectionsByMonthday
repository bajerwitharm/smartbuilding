select count(*) AS connections, name, account, (strftime('%d',connections.timestamp)) as monthday from users left join connections on connections.user_id=users.id where result='OK' {1} group by monthday, mac order by connections desc;