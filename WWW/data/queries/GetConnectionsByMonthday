select count(*) AS connections, name, account, (DATE_FORMAT(connections.timestamp,'%d')) as monthday from users left join connections on connections.user_id=users.id where result='OK' {1} group by monthday, mac order by connections desc;