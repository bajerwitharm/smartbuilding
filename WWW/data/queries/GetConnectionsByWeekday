select count(*) AS connections, name, account, (DATE_FORMAT(connections.timestamp,'%w')) as weekday from users left join connections on connections.user_id=users.id where result='OK' {1} group by weekday, mac order by connections desc;